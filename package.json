{
  "name": "dateam",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "shared"
  ],
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=7.0.0"
  },
  "scripts": {
    "build": "cd ./shared && npm run build && cd ../frontend && npm run build && cd .. && cd ./backend && npm run build ",
    "start": "cd ./backend && npm start",
    "test": "echo 'Whoops! You need to cd into either frontend/ or backend/ to run scripts'",
    "docker": "docker-compose --env-file ./backend/.env up"
  }
}
